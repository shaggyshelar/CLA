// import { fromJS } from 'immutable';
// import { makeSelectSegmentedQuoteDomain } from '../selectors';

// const selector = makeSelectSegmentedQuoteDomain();

describe('makeSelectSegmentedQuoteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
