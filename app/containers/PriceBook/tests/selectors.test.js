// import { fromJS } from 'immutable';
// import { makeSelectPriceBookDomain } from '../selectors';

// const selector = makeSelectPriceBookDomain();

describe('makeSelectPriceBookDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
