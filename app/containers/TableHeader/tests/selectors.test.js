// import { fromJS } from 'immutable';
// import { makeSelectTableHeaderDomain } from '../selectors';

// const selector = makeSelectTableHeaderDomain();

describe('makeSelectTableHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
