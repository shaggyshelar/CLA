// import React from 'react';
// import { shallow } from 'enzyme';

// import { AddConfigureProducts } from '../index';

describe('<AddConfigureProducts />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
