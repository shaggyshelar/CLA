// import { fromJS } from 'immutable';
// import { makeSelectAddConfigureProductsDomain } from '../selectors';

// const selector = makeSelectAddConfigureProductsDomain();

describe('makeSelectAddConfigureProductsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
