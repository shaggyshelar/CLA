// import { fromJS } from 'immutable';
// import { makeSelectEditQuoteDomain } from '../selectors';

// const selector = makeSelectEditQuoteDomain();

describe('makeSelectEditQuoteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
