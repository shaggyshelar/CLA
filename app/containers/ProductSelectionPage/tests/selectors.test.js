// import { fromJS } from 'immutable';
// import { makeSelectProductSelectionPageDomain } from '../selectors';

// const selector = makeSelectProductSelectionPageDomain();

describe('makeSelectProductSelectionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
