// import React from 'react';
// import { shallow } from 'enzyme';

// import { ProductSelectionHeader } from '../index';

describe('<ProductSelectionHeader />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
