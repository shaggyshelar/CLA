// import { fromJS } from 'immutable';
// import { makeSelectProductSelectionHeaderDomain } from '../selectors';

// const selector = makeSelectProductSelectionHeaderDomain();

describe('makeSelectProductSelectionHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
