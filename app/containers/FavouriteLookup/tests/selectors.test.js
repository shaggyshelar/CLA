// import { fromJS } from 'immutable';
// import { makeSelectFavouriteLookupDomain } from '../selectors';

// const selector = makeSelectFavouriteLookupDomain();

describe('makeSelectFavouriteLookupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
