// import { fromJS } from 'immutable';
// import { makeSelectReConfigureProductsDomain } from '../selectors';

// const selector = makeSelectReConfigureProductsDomain();

describe('makeSelectReConfigureProductsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
