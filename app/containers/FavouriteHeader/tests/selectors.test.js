// import { fromJS } from 'immutable';
// import { makeSelectFavouriteHeaderDomain } from '../selectors';

// const selector = makeSelectFavouriteHeaderDomain();

describe('makeSelectFavouriteHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
