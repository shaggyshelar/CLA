// import { fromJS } from 'immutable';
// import { makeSelectEditQuoteHeaderDomain } from '../selectors';

// const selector = makeSelectEditQuoteHeaderDomain();

describe('makeSelectEditQuoteHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
