// import { fromJS } from 'immutable';
// import { makeSelectSuggestionPageDomain } from '../selectors';

// const selector = makeSelectSuggestionPageDomain();

describe('makeSelectSuggestionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
