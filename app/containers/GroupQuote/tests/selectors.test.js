// import { fromJS } from 'immutable';
// import { makeSelectGroupQuoteDomain } from '../selectors';

// const selector = makeSelectGroupQuoteDomain();

describe('makeSelectGroupQuoteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
