// import React from 'react';
// import { shallow } from 'enzyme';

// import SegmentedEditQuoteGrid from '../index';

describe('<SegmentedEditQuoteGrid />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
