// import React from 'react';
// import { shallow } from 'enzyme';

// import AddconfigureProductHeader from '../index';

describe('<AddconfigureProductHeader />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
