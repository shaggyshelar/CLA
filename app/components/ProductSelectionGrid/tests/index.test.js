// import React from 'react';
// import { shallow } from 'enzyme';

// import ProductSelectionGrid from '../index';

describe('<ProductSelectionGrid />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
